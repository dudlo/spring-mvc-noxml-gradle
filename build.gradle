apply plugin: 'war'
apply plugin: 'eclipse-wtp'

repositories {
    mavenCentral()
}

dependencies {
    compile 'javax.servlet:javax.servlet-api:3.0.1'   // 3.0 is required for Java-based configuration; 3.1 would not work in Tomcat 7
    compile 'org.springframework:spring-webmvc:4.0.+'
}